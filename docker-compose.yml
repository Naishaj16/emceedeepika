name: emceedeepika

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: emceedeepika_app
    ports:
      - "5173:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
